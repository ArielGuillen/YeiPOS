<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yeipos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutUs"/>
        <activity android:name=".Ayuda" />
        <activity android:name=".TerminarOrden" />
        <activity android:name=".ModificarOrden" />
        <activity android:name=".ModificarInventario" />
        <activity android:name=".AddToInventario" />
        <activity
            android:name=".login_actividad"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AgregarOrden"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity"/>
        <activity
            android:name=".users.AgregarUsuarios"
            android:label="@string/title_activity_add"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <meta-data
            android:name="android.support.PARENT_ACTIVITY"
            android:value=".MainActivity" />

        <activity
            android:name=".RegistroDeVentas"
            android:label="@string/ventas" />
    </application>

</manifest>